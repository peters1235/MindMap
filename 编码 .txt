unicode相当于中文, UTF-8, UTF-16等相当于 行书, 楷书, 草书等各种书写方式.

Unicode 用2个字节来表示一个字符，对于英文字母会浪费许多空间，16位里只用到7位。
于是用UTF-8来重新编码，实现变长，以节省空间
UTF-8用1-6个字节来表示一个字符 ，汉字一般3个字节
如果只用一个字节的话，第一位置0 
如果用n>1个字节的话，第一位是 n个1加一个0 ，后面每个字节的开头两位都是10 ，一个字节里剩下的地方从低位开始用来填充Unicode码，再空出来的位置用0来填充。